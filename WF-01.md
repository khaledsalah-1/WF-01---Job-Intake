# WF-01 — Job Intake

**n8n Workflow ID:** `7l5CBLmM6Mo5TlsC`
**رابط الـ Workflow:** https://paradox-domain-canal.ngrok-free.dev/workflow/7l5CBLmM6Mo5TlsC

---

## 1. الهدف

استقبال إعلانات الوظائف من أي مصدر (Webhook، Telegram، Email)، توحيدها في هيكل بيانات واحد (Standard Job Schema)، تحليلها بواسطة AI لاستخراج بيانات منظّمة **من غير اختلاق أي معلومة غير موجودة في النص الأصلي**، منع التكرار (Deduplication) بشكل Atomic، وتخزينها في PostgreSQL بحالة `new` جاهزة تستلمها WF-02 (Job Analysis).

هذا الـ Workflow هو **نقطة الدخول الوحيدة** لأي وظيفة في النظام كله — أي مصدر تاني (WF-10 Job Discovery مثلًا) لازم يمر منطقيًا بنفس مبدأ التوحيد والـ Dedup الموجود هنا، حتى لو كان عنده Trigger منفصل.

---

## 2. Triggers (3 نقاط دخول مستقلة)

| # | اسم الـ Node | النوع | الوصف |
|---|---|---|---|
| 1 | Webhook - Job Intake | `n8n-nodes-base.webhook` | استقبال POST يحتوي JSON، أو نص خام، أو رابط وظيفة، أو ملف PDF مرفوع كـ Binary |
| 2 | Telegram - Job Intake | `n8n-nodes-base.telegramTrigger` | استقبال رسائل تليجرام: نص/رابط، أو مستند PDF |
| 3 | Email - Job Intake (IMAP) | `n8n-nodes-base.emailReadImap` | مراقبة صندوق بريد مخصص لاستقبال إعلانات الوظائف (نص الإيميل أو مرفق PDF) |

**لماذا 3 Triggers منفصلة بدل واحد موحّد؟**
كل مصدر له شكل Payload مختلف تمامًا (Webhook body، Telegram message object، IMAP email object)، ومحاولة توحيدها في Trigger واحد مستحيلة تقنيًا في n8n. الحل هو تطبيع كل مصدر لوحده عبر Code Node مخصص، ثم توحيدهم كلهم عند نقطة التقاء واحدة (`Route by Content Type`).

---

## 3. كل Node بالترتيب

### 3.1 فرع Webhook

| Node | النوع | الإعدادات الأساسية |
|---|---|---|
| `Webhook - Job Intake` | Webhook | `httpMethod: POST`, `path: job-intake`, `responseMode: onReceived` |
| `Classify Webhook Source` | Code | يفحص وجود Binary (→ pdf)، أو `job_url` (→ url)، أو نص خام (→ text) |

**Input المتوقع (JSON عبر POST):**
```json
{
  "raw_text": "We are hiring a Backend Engineer with 3+ years experience in Python and PostgreSQL.",
  "job_url": ""
}
```
أو لرابط وظيفة:
```json
{ "job_url": "https://example.com/jobs/99" }
```
أو رفع ملف PDF مباشرة كـ Binary Body.

**Output من `Classify Webhook Source`:**
```json
{ "contentType": "text", "rawText": "We are hiring...", "jobUrl": "", "sourceType": "webhook" }
```

### 3.2 فرع Telegram

| Node | النوع | الإعدادات الأساسية |
|---|---|---|
| `Telegram - Job Intake` | Telegram Trigger | `updates: [message]` |
| `Telegram: Has Document?` | IF | يفحص `{{ $json.message.document ? true : false }}` |
| **True →** `Download Telegram File` | Telegram (resource: file) | `fileId: {{ $json.message.document.file_id }}` |
| → `Set Telegram PDF Meta` | Set | يضبط `contentType=pdf`, `sourceType=telegram` |
| **False →** `Classify Telegram Text` | Code | يفحص لو النص رابط (Regex `^https?://`) → url، وإلا → text |

**لماذا IF منفصلة قبل التصنيف؟**
لازم نحمّل ملف الـ PDF فعليًا (Binary) من سيرفرات تليجرام قبل ما نقدر نبعته لعقدة استخراج النص — وده مش متاح للرسائل النصية، فالتفريع هنا إجباري تقنيًا مش اختياري.

### 3.3 فرع Email (IMAP)

| Node | النوع | الإعدادات الأساسية |
|---|---|---|
| `Email - Job Intake (IMAP)` | Email Read IMAP | `downloadAttachments: true` |
| `Classify Email Source` | Code | لو فيه Binary attachment → pdf، وإلا يقرأ `$json.text` → text |

### 3.4 نقطة الالتقاء المشتركة

| Node | النوع | الوصف |
|---|---|---|
| `Route by Content Type` | Switch | يوجّه حسب `$json.contentType`: pdf / url / text / **Unclassified (fallback)** |

**فرع PDF:**
`Extract Text from PDF` (Extract from File، `operation: pdf`) → `Set Raw Text (PDF)` (`rawText = {{ $json.data }}`, `includeOtherFields: true`) → `Normalize Job Payload`

**فرع URL:**
`Fetch Job URL Page` (HTTP GET، `url: {{ $json.jobUrl }}`, Retry: 3 محاولات) → `Extract Text from HTML` (`cssSelector: body`) → `Set Raw Text (URL)` (`rawText = {{ $json.bodyText }}`) → `Normalize Job Payload`

**فرع Text:** يذهب مباشرة إلى `Normalize Job Payload` (البيانات جاهزة بالفعل).

**فرع Unclassified (Fallback):** `Log Unclassified Source` (Postgres insert في `workflow_logs`) → `Notify Admin - Unclassified Source` (Telegram).

### 3.5 التحليل بالـ AI

| Node | النوع | الإعدادات |
|---|---|---|
| `Normalize Job Payload` | Set | يضمن وجود `rawText`, `sourceType`, `jobUrl` بشكل موحّد بغض النظر عن المصدر |
| `AI Job Analyzer - Structured Extraction` | AI Agent (Claude) | `systemMessage` صارم يمنع الاختلاق + `hasOutputParser: true` |
| ↳ Subnode: `Claude Model` | Anthropic Chat Model | `model: claude-sonnet-4-6` |
| ↳ Subnode: `Job Schema Parser` | Structured Output Parser | Schema JSON مطابق لهيكل الوظيفة القياسي |

**الـ Prompt (System Message):**
> "You are a precise job-posting data extractor... You never infer, guess, or fabricate company names, salaries, skills, or requirements that are not present in the source text."

**الـ Prompt (User Message) — Expression:**
```
Extract structured job posting information from the text below. Follow these rules strictly:
- Do NOT invent or assume any information that is not explicitly present in the text.
- If a field is not mentioned, leave it as an empty string or an empty array.
- Return valid JSON matching the required schema only.

Job posting source: {{ $json.sourceType }}
Job URL (if any): {{ $json.jobUrl }}

Raw job posting text:
{{ $json.rawText }}
```

**Output (JSON):**
```json
{
  "company": "Acme Corp",
  "job_title": "Backend Engineer",
  "location": "Remote",
  "employment_type": "full-time",
  "experience_required": "3-5 years",
  "salary": "80000-100000 USD",
  "skills": ["Python", "PostgreSQL"],
  "responsibilities": ["Build REST APIs"],
  "requirements": ["University degree"],
  "source": "webhook",
  "job_url": ""
}
```

### 3.6 منع التكرار + التخزين

| Node | النوع | الإعدادات |
|---|---|---|
| `Compute Dedup Hash` | Crypto (SHA256) | `value: {{ ($json.company||"").toLowerCase() }}\|{{ ($json.job_title||"").toLowerCase() }}\|{{ $json.job_url||"" }}` |
| `Insert Job If New (Dedup)` | Postgres | `INSERT ... ON CONFLICT (dedup_hash) DO NOTHING RETURNING id` — Retry: 3 محاولات |
| `Ready for WF-02 Job Analysis` | NoOp | Placeholder — يُستبدل بعقدة Execute Workflow فور بناء WF-02 |
| `Notify Admin - New Job Saved` | Telegram | إشعار بنجاح الحفظ |

**لماذا `ON CONFLICT DO NOTHING RETURNING id` بدل SELECT ثم INSERT؟**
لو استخدمنا استعلام SELECT منفصل للتحقق من التكرار قبل الـ INSERT، بيبقى فيه فجوة زمنية (Race Condition) — لو نفس الوظيفة وصلت من مصدرين في نفس اللحظة (مثلاً Webhook + Job Discovery)، الاتنين ممكن يعدوا فحص التكرار في نفس الوقت قبل ما أي حد منهم يعمل Insert. الحل الذري (Atomic) هو خلي قاعدة البيانات نفسها تتولى منع التكرار عبر الـ UNIQUE constraint على `dedup_hash`، والـ Workflow بس بيقرأ النتيجة: لو رجع صف (`id` موجود) يبقى وظيفة جديدة، ولو رجع صفر صفوف يبقى مكررة والـ Workflow بيتوقف بشكل طبيعي من غير أي Error.

---

## 4. Error Handling

| الآلية | التطبيق |
|---|---|
| **Retry Logic** | `retryOnFail: true`, `maxTries: 3` على: `Fetch Job URL Page`, `Insert Job If New`, `Log Unclassified Source` |
| **Continue on Error** | كل عقد التنبيه (Telegram) مضبوطة على `onError: continueRegularOutput` — فشل الإشعار ما يوقفش تخزين الوظيفة |
| **Fallback Path** | أي محتوى ما ينفعش يتصنّف يروح لـ `Log Unclassified Source` + تنبيه أدمن، بدل ما يختفي بصمت |
| **Zero-Item Safety** | تكرار الوظيفة بيطلع 0 صفوف من `Insert Job If New`، فالـ Workflow يتوقف بشكل طبيعي (مفيش Error، ده سلوك متوقع) |
| **Error Trigger مركزي (WF-12)** | هيتم ربطه في `Workflow Settings → Error Workflow` بعد بناء WF-12 |

---

## 5. Testing Steps

1. **اختبار نص خام عبر Webhook:**
   ```bash
   curl -X POST https://<n8n-host>/webhook/job-intake \
     -H "Content-Type: application/json" \
     -d '{"raw_text": "We are hiring a Senior Python Developer, remote, 5+ years experience."}'
   ```
   تحقق: صف جديد في جدول `jobs` بحالة `new`، و`analysis_json` معبّى.

2. **اختبار Dedup:** أرسل نفس الطلب اللي فوق مرة تانية بالضبط → تحقق إن مفيش صف تاني اتضاف (نفس `dedup_hash`).

3. **اختبار رابط وظيفة:** أرسل `{"job_url": "<رابط حقيقي>"}` → تحقق إن `Fetch Job URL Page` رجع HTML، والنص اتستخرج صح.

4. **اختبار Telegram:** ابعت رسالة نصية فيها وصف وظيفة للبوت → تحقق من نفس النتيجة.

5. **اختبار مصدر غير مصنّف:** ابعت Payload فاضي `{}` → تحقق إنه راح لفرع `Unclassified` وسجّل في `workflow_logs` وبعت تنبيه.

6. **اختبار الفشل المتحكم فيه:** افصل الإنترنت مؤقتًا وقت تشغيل `Fetch Job URL Page` → تحقق إنه عمل Retry 3 مرات قبل ما يكمل بـ Error محتوى بدل ما يوقف الـ Workflow كله.

---

## 6. الخطوات التالية قبل التشغيل الفعلي

- [ ] ربط Credential باسم **"Postgres - Job Automation DB"**
- [ ] ربط Credential باسم **"Anthropic Claude API"**
- [ ] ربط Credential باسم **"Job Intake Mailbox (IMAP)"**
- [ ] ضبط قيمة `placeholder` الخاصة بـ `TELEGRAM_ADMIN_CHAT_ID` في عقدتي التنبيه
- [ ] تشغيل SQL schema (القسم 2 من `ARCHITECTURE.md`) على قاعدة البيانات
- [ ] تفعيل (Publish) الـ Workflow من n8n

بعد ما تربط الـ Credentials دي وتجرب الاختبارات فوق، جاهزين نبدأ **WF-02 Job Analysis** (اللي هتوصله البيانات المخزّنة من هنا).
